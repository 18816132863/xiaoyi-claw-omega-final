# V7.2.0 第一阶段基线冻结

## 冻结时间
2026-04-16 10:26

## 通过命令
```bash
$ python tests/integration/test_minimum_loop.py
Ran 12 tests in 0.007s - OK

$ python scripts/demo_minimum_loop.py
✅ 最小闭环演示完成
```

## 冻结接口

### skills 平台
```python
from skills.registry.skill_registry import SkillRegistry, SkillManifest, SkillCategory, SkillStatus
from skills.runtime.skill_router import SkillRouter, SkillExecutionContext, SkillExecutionResult
from skills.lifecycle.lifecycle_manager import LifecycleManager, LifecycleState, LifecycleEvent
```

### memory_context
```python
from memory_context.builder.context_builder import ContextBuilder, ContextBundle, build_context
```

### orchestration
```python
from orchestration.workflow.workflow_engine import WorkflowEngine, run_workflow, WorkflowResult, StepResult
```

### governance
```python
from governance.control_plane.policy_engine import PolicyEngine, Policy, PolicyType, PolicyEffect
```

## 冻结文件

| 文件 | 说明 |
|------|------|
| `skills/registry/skill_registry.py` | 技能注册中心 |
| `skills/runtime/skill_router.py` | 技能路由器 |
| `skills/lifecycle/lifecycle_manager.py` | 生命周期管理 |
| `memory_context/builder/context_builder.py` | 上下文构建器 |
| `orchestration/workflow/workflow_engine.py` | 工作流引擎 |
| `governance/control_plane/policy_engine.py` | 策略引擎 |
| `tests/integration/test_minimum_loop.py` | 集成测试 |
| `scripts/demo_minimum_loop.py` | 最小闭环演示 |

## 不允许随便改动的入口

1. **skills 导入路径** - 不允许改
2. **SkillRegistry/SkillRouter/LifecycleManager 类签名** - 不允许改
3. **ContextBuilder.build_context() 签名** - 不允许改
4. **WorkflowEngine.run_workflow() 签名** - 不允许改
5. **PolicyEngine.evaluate_policy() 签名** - 不允许改
6. **test_minimum_loop.py 导入路径** - 不允许改
7. **demo_minimum_loop.py 导入路径** - 不允许改

## 阶段回归检查

```bash
make verify-phase1-baseline
```

等价于：
```bash
python tests/integration/test_minimum_loop.py
python scripts/demo_minimum_loop.py
```

第二阶段开发中，必须保证这两条命令持续通过。
